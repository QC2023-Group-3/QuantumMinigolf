from .subBall import subBall

def initializeBalls(amtOfBalls: int) -> list[subBall]:
	# Returns a list of subBalls, where the amtOfBalls is the amount of subBalls in the list

	return [] # Change later

def pickFinal() -> subBall:
	# Return the final subBall that is picked to determine if you have won or not

	return subBall